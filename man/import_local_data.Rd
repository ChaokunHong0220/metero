% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_import.R
\name{import_local_data}
\alias{import_local_data}
\title{Import local AMR data and provide quality assessment}
\usage{
import_local_data(
  file_path = NULL,
  data = NULL,
  file_type = NULL,
  mapping = NULL,
  domain = c("human", "animal", "environment"),
  ...
)
}
\arguments{
\item{file_path}{Path to the data file, or NULL if providing a data frame directly}

\item{data}{Optional data frame containing AMR data (if providing data directly)}

\item{file_type}{Type of file: "csv", "excel", "rdata", or NULL for auto-detect}

\item{mapping}{A named list mapping source column names to standard names}

\item{domain}{Domain of the data: "human", "animal", "environment"}

\item{...}{Additional parameters passed to import_amr_data}
}
\value{
A list containing the imported data and a quality assessment report
}
\description{
Imports local AMR data (either from a file or a direct data frame) and performs
basic quality checks, providing a report on data completeness and potential issues.
}
\examples{
\dontrun{
# Example 1: Import and assess local file data quality
mapping <- list(
  study_id = "Study_ID",
  pathogen = "Bacteria",
  specific_antibiotic = "Antibiotic",
  resistance_rate = "Resistance_Rate"
)

result <- import_local_data(
  file_path = "local_hospital_data.csv",
  mapping = mapping,
  domain = "human"
)

# View quality assessment
print(result$quality_assessment)

# Example 2: Assessment with direct data input
my_df <- data.frame(
  Study_ID = c("Study1", "Study1", "Study2"),
  Bacteria = c("E. coli", "S. aureus", "K. pneumoniae"),
  Antibiotic = c("Ciprofloxacin", "Oxacillin", "Meropenem"),
  Total = c(100, 50, 75),
  Resistant = c(34, 22, 12)
)

result <- import_local_data(
  data = my_df,
  mapping = mapping,
  domain = "human"
)
}
}
