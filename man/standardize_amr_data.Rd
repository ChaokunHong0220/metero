% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_import.R, R/data_standardization.R
\name{standardize_amr_data}
\alias{standardize_amr_data}
\title{Enhanced standardize AMR data format}
\usage{
standardize_amr_data(data, mapping, validate = TRUE)

standardize_amr_data(data, mapping, validate = TRUE)
}
\arguments{
\item{data}{A data frame containing AMR data}

\item{mapping}{A named list mapping user's column names to standard names}

\item{validate}{Logical; whether to validate the data structure (default: TRUE)}

\item{domain}{Domain of the data: "human", "animal", "environment"}

\item{domain_schema}{Optional domain-specific schema}

\item{standardize_categorical}{Logical; whether to standardize categorical variables}
}
\value{
A data frame with standardized variable names and structure

A data frame with standardized variable names and structure
}
\description{
This function extends the basic standardize_amr_data function with additional
validation and processing capabilities, including domain-specific standardization.

This function converts user-provided AMR data to the standard format used by metero.
It performs validation and standardization of variable names and data types.
}
\examples{
\dontrun{
# Create a mapping from your data to the standard format
my_mapping <- list(
  "Study ID" = "study_id",
  "Bacteria" = "pathogen",
  "Drug" = "specific_antibiotic",
  "Total" = "sample_count",
  "Resistant" = "resistant_count"
)

# Standardize your data
std_data <- standardize_amr_data(
  my_data, 
  my_mapping, 
  domain = "human",
  standardize_categorical = TRUE
)
}
\dontrun{
# Create a mapping from your data to the standard format
my_mapping <- list(
  "Study ID" = "study_id",
  "Bacteria" = "pathogen_name",
  "Drug" = "antibiotic_name"
)

# Standardize your data
std_data <- standardize_amr_data(my_data, my_mapping)
}
}
