% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_import.R
\name{import_amr_data}
\alias{import_amr_data}
\title{Import AMR data from various file formats or directly from a data frame}
\usage{
import_amr_data(
  file_path = NULL,
  data = NULL,
  file_type = NULL,
  sheet = 1,
  mapping = NULL,
  domain = c("human", "animal", "environment"),
  header = TRUE,
  skip = 0,
  validate = TRUE,
  ...
)
}
\arguments{
\item{file_path}{Path to the data file, or NULL if providing a data frame directly}

\item{data}{Optional data frame containing AMR data (if providing data directly)}

\item{file_type}{Type of file: "csv", "excel", "rdata", or NULL for auto-detect}

\item{sheet}{Sheet name or number (for Excel files)}

\item{mapping}{A named list mapping source column names to standard names}

\item{domain}{Domain of the data: "human", "animal", "environment"}

\item{header}{Logical; whether the file has a header row (for CSV)}

\item{skip}{Number of rows to skip before the header (for CSV and Excel)}

\item{validate}{Logical; whether to validate the imported data}

\item{...}{Additional parameters passed to read functions}
}
\value{
A data frame containing the imported data
}
\description{
Imports antimicrobial resistance data from CSV, Excel, RData files, or directly from an existing data frame,
and provides options for initial validation and standardization.
}
\examples{
\dontrun{
# Example 1: Import CSV data with automatic standardization
mapping <- list(
  study_id = "Study_ID",
  pathogen = "Bacteria",
  specific_antibiotic = "Antibiotic",
  resistance_rate = "Resistance_Rate"
)

human_data <- import_amr_data(
  file_path = "my_amr_data.csv",
  mapping = mapping,
  domain = "human"
)

# Example 2: Directly use an existing data frame
my_df <- data.frame(
  Study_ID = c("Study1", "Study1", "Study2"),
  Bacteria = c("E. coli", "S. aureus", "K. pneumoniae"),
  Antibiotic = c("Ciprofloxacin", "Oxacillin", "Meropenem"),
  Total = c(100, 50, 75),
  Resistant = c(34, 22, 12)
)

processed_data <- import_amr_data(
  data = my_df,
  mapping = mapping,
  domain = "human"
)
}
}
