% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend_analysis.R, R/visualization.R
\name{create_amr_heatmap}
\alias{create_amr_heatmap}
\title{Create antimicrobial resistance heatmap}
\usage{
create_amr_heatmap(
  data,
  filter_conditions = NULL,
  sort_pathogens = c("resistance", "alphabetical", NULL),
  sort_antibiotics = c("class", "resistance", "alphabetical", NULL),
  color_palette = c("viridis", "red_blue", "red_green", "custom"),
  na_color = "gray90",
  label_format = "\%.1f\%\%",
  threshold = 0.5,
  min_studies = 1,
  pathogen_col = "pathogen_name",
  antibiotic_col = "antibiotic_name"
)

create_amr_heatmap(
  data,
  filter_conditions = NULL,
  sort_pathogens = c("resistance", "alphabetical", NULL),
  sort_antibiotics = c("class", "resistance", "alphabetical", NULL),
  color_palette = c("viridis", "red_blue", "red_green", "custom"),
  na_color = "gray90",
  label_format = "\%.1f\%\%",
  threshold = 0.5,
  min_studies = 1,
  pathogen_col = "pathogen_name",
  antibiotic_col = "antibiotic_name"
)
}
\arguments{
\item{data}{A data frame containing AMR data}

\item{filter_conditions}{Optional list of filtering conditions}

\item{sort_pathogens}{Method to sort pathogens: "alphabetical", "resistance", or NULL}

\item{sort_antibiotics}{Method to sort antibiotics: "alphabetical", "resistance", "class", or NULL}

\item{color_palette}{Color palette for the heatmap}

\item{na_color}{Color for missing values}

\item{label_format}{Format string for cell labels (e.g., "\%.1f\%\%")}

\item{threshold}{Resistance rate threshold for color differentiation}

\item{min_studies}{Minimum number of studies required to show a cell}

\item{pathogen_col}{Name of the column containing pathogen information (default: "pathogen_name")}

\item{antibiotic_col}{Name of the column containing antibiotic information (default: "antibiotic_name")}

\item{pathogen_var}{Pathogen variable name, default is "pathogen_name"}

\item{antibiotic_var}{Antibiotic variable name, default is "antibiotic_name"}
}
\value{
ggplot2 heatmap object

A ggplot2 object with the heatmap
}
\description{
This function generates a heatmap showing resistance patterns between different pathogens and antibiotics.

Generates a heatmap visualizing antimicrobial resistance rates
across different pathogens and antibiotics.
}
\examples{
\dontrun{
data(human_amr)
# Create heatmap of all data
heatmap <- create_amr_heatmap(human_amr)

# Create heatmap for a specific country
india_heatmap <- create_amr_heatmap(
  human_amr, 
  filter_conditions = list(country = "India")
)

# Show heatmap
print(heatmap)
}
\dontrun{
# Create a basic resistance heatmap
heatmap <- create_amr_heatmap(amr_data)

# Create a filtered heatmap for a specific country
heatmap <- create_amr_heatmap(
  amr_data,
  filter_conditions = list(country = "India")
)

# Specify custom column names
heatmap <- create_amr_heatmap(
  amr_data,
  pathogen_col = "pathogen_name",
  antibiotic_col = "antibiotic_name"
)
}
}
